<div>
  <%= link_to 'Restaurant', admin_restaurants_path %> |
  <%= link_to 'Categories', admin_categories_path %>
</div>


<div class="container">

  <h1>餐廳後台</h1>

  <div class="row">

    <div class="col-md-12">

      <div>
        <%= link_to 'New Restaurant', new_admin_restaurant_path, class: "btn btn-primary" %>
      </div>
      <br>
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>No.</th>
            <th>Category</th>
            <th>Name</th>
            <th>Function</th>
          </tr>
        </thead>
        <tbody>
          <% @restaurants.each do |restaurant| %>
          <tr>
            <th scope="row"><%= restaurant.id %></th>
            <td>[<%= restaurant.category.name %>]</td>
            <td><%= restaurant.name %></td>
            <td>
<div class="btn-group" role="group" aria-label="Basic example">
  <button type="button" class="btn btn-secondary"><%= link_to 'Show', admin_restaurant_path(restaurant) %></button>
  <button type="button" class="btn btn-secondary"><%= link_to 'Edit', edit_admin_restaurant_path(restaurant) %></button>
  <button type="button" class="btn btn-secondary"><%= link_to 'Delete', admin_restaurant_path(restaurant), method: :delete, data: { confirm: "Are you sure?"} %></button>
</div>



              
              
              
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <div class="text-center">
        <%= paginate @restaurants %>
      </div>

    </div>

  </div>
</div>